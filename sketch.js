function setup() {
  createCanvas(400, 269);
  background(150);

  beginShape();
  vertex(10, 10);
  vertex(50, 10);
  vertex(50, 50);
  vertex(25, 100);
  vertex(30, 70);
  endShape();

  // strokeWeight(50);
  // point(width / 2, height / 2);
  // strokeWeight(1);
  // ellipse(10, 10, 10);
  // strokeWeight(10);
  // line(100, 0, width - 10, height - 100);
  // line(0, 0, width - 100, height);
  // noStroke();
  // fill(0, 255, 0, 255);
  // rect(50, 50, 50, 50);
  // // fill("red");
  // stroke("orange");
  // noFill();
  // arc(25, 25, 100, 100, radians(0), radians(90));

  // stroke("purple");

  // point()

  // curve(-1000, -1000, 0, height / 2, width, height / 2, width - 1000, height - 1000);
}

function draw() {

}